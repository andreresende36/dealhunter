# =============================================================================
# DealHunter — Dependências Python
# Instalar com: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Web Scraping
# -----------------------------------------------------------------------------
playwright==1.49.0           # Automação de browser (Chromium headless)
beautifulsoup4==4.12.3       # Parsing de HTML
lxml==5.3.0                  # Parser XML/HTML rápido (backend para BS4)
httpx>=0.26,<0.28            # Cliente HTTP assíncrono
fake-useragent==1.5.1        # Geração de User-Agent realistas
playwright-stealth==1.0.6    # Esconder sinais de automação do Playwright

# -----------------------------------------------------------------------------
# Banco de Dados
# -----------------------------------------------------------------------------
supabase==2.10.0             # Cliente oficial Supabase (PostgreSQL)
postgrest>=0.18.0,<0.19.0    # Cliente PostgREST (dependência do supabase-py)
aiosqlite==0.20.0            # SQLite assíncrono (fallback local)
bcrypt>=4.0,<5.0             # Hash de senha (Argon2 não necessário, bcrypt suficiente)

# -----------------------------------------------------------------------------
# IA — Claude API
# -----------------------------------------------------------------------------
anthropic==0.42.0            # SDK oficial Anthropic

# -----------------------------------------------------------------------------
# Telegram
# -----------------------------------------------------------------------------
python-telegram-bot==21.9    # Bot Telegram assíncrono

# -----------------------------------------------------------------------------
# WhatsApp (Evolution API — chamadas HTTP diretas via httpx)
# -----------------------------------------------------------------------------
# Sem SDK dedicado; usar httpx para chamadas REST

# -----------------------------------------------------------------------------
# Configuração
# -----------------------------------------------------------------------------
python-dotenv==1.0.1         # Leitura de .env

# -----------------------------------------------------------------------------
# Utilitários
# -----------------------------------------------------------------------------
tenacity==9.0.0              # Retry com backoff exponencial
structlog==24.4.0            # Logging estruturado

# -----------------------------------------------------------------------------
# Testes
# -----------------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-mock==3.14.0
respx==0.21.1                # Mock de chamadas httpx em testes

# -----------------------------------------------------------------------------
# Desenvolvimento
# -----------------------------------------------------------------------------
ruff==0.9.2                  # Linter + formatter ultra-rápido
mypy==1.14.1                 # Type checking
ipython==8.31.0              # Shell interativo para debug
